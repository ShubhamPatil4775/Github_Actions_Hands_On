name: Java Setup

on:
  workflow_dispatch: 
jobs:
  java-setup:
    runs-on: ubuntu-latest
    steps:
      - name: Setup Java JDK
        uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: microsoft
      - name: Print Java Version
        run: java -version
