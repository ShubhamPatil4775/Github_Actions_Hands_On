name: Python Setup

on:
  workflow_dispatch: 

jobs:
  python-setup:
    runs-on: ubuntu-latest
    steps:
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9' 
      - name: Print Python Version
        run: python --version 
