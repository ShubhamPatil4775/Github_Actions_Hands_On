name: Node.js Setup

on:
  workflow_dispatch: 

jobs:
  nodejs-setup:
    runs-on: ubuntu-latest
    steps:
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Print Node.js Version
        run: node --version
